import { Flex, Text } from "@chakra-ui/react";

const HomePage: React.FC = () => {
    return (
        <Flex
            w="full"
            h="full"
            direction="column"
            align="center"
            justify="center"
        >
            <Text>Home Page</Text>
        </Flex>
    );
};

export default HomePage;
